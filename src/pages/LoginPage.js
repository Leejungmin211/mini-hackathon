import styled from 'styled-components';

const Section = styled.section`
  margin-top: 20px;
`;

export default function LoginPage() {
  return (
    <Section>
      <div>LoginPage</div>
    </Section>
  );
}
