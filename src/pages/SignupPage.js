import styled from 'styled-components';

const Section = styled.section`
  margin-top: 20px;
`;

export default function SignupPage() {
  return (
    <Section>
      <div>SignupPage</div>
    </Section>
  );
}
